//Determine Organization Model
//Define trnaslplant center slection
//Make Organization call
//Retrive Organization details and endpoints(shared gobally) if exists and matched(rank 1) with iss received from Client.

import { Injectable } from "@angular/core";

@Injectable()
export class OrganizationService {
  constructor() {}
}
